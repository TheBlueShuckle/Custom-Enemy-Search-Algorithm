public class AStar
{
    private int[,] _map;

    public AStar(int[,] map)
    {
        _map = map;
    }

    public float[] GetOptimalPath(int[] start, int[] target, int cost)
    {
        float[] vector = { 1, 2 };
        return vector;
    }
}
